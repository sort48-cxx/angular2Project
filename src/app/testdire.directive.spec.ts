import { TestdireDirective } from './testdire.directive';

describe('TestdireDirective', () => {
  it('should create an instance', () => {
    const directive = new TestdireDirective();
    expect(directive).toBeTruthy();
  });
});
